extends ~@/layout
block title
  title

block content

  - const data = require('./data.js')
  - const css = require('./_css.json')

  div(id='index-pg' class=css.indexPg)

    +hero(css.indexPgHero)

    +form-card(css.indexPgFormCard)

    +gallery(css.indexPgGallery)

    +price(css.indexPgPrice)

    +form-card(css.indexPgFormCard)

    +contacts(css.indexPgContacts)

    +footer
